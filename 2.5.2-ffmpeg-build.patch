--- origsrc/avidemux_2.5.2/cmake/admFFmpegBuild.cmake
+++ src/avidemux_2.5.2/cmake/admFFmpegBuild.cmake
@@ -129,11 +129,11 @@ add_custom_target(ffmpeg ALL
 # Add and install libraries
 getFfmpegLibNames("${FFMPEG_SOURCE_DIR}")
 
-if (WIN32)
+if (WIN32 OR CYGWIN)
 	set(FFMPEG_INSTALL_DIR ${BIN_DIR})
-else (WIN32)
+else (WIN32 OR CYGWIN)
 	set(FFMPEG_INSTALL_DIR lib${LIB_SUFFIX})
-endif (WIN32)
+endif (WIN32 OR CYGWIN)
 
 add_library(ADM_libswscale UNKNOWN IMPORTED)
 set_property(TARGET ADM_libswscale PROPERTY IMPORTED_LOCATION "${FFMPEG_BINARY_DIR}/libswscale/${LIBSWSCALE_LIB}")
--- origsrc/avidemux_2.5.1/cmake/admFFmpegUtil.cmake	2009-05-23 15:19:56.000000000 -0500
+++ src/avidemux_2.5.1/cmake/admFFmpegUtil.cmake	2009-11-10 20:42:59.135447500 -0600
@@ -21,6 +21,12 @@ MACRO (getFfmpegLibNames sourceDir)
 		set(LIBAVUTIL_LIB lib${LIBAVCODEC_ADM}avutil.${LIBAVUTIL_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
 		set(LIBPOSTPROC_LIB lib${LIBAVCODEC_ADM}postproc.${LIBPOSTPROC_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
 		set(LIBSWSCALE_LIB lib${LIBAVCODEC_ADM}swscale.${LIBSWSCALE_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
+	elseif (CYGWIN)
+		set(LIBAVCODEC_LIB cyg${LIBAVCODEC_ADM}avcodec-${LIBAVCODEC_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
+		set(LIBAVFORMAT_LIB cyg${LIBAVCODEC_ADM}avformat-${LIBAVFORMAT_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
+		set(LIBAVUTIL_LIB cyg${LIBAVCODEC_ADM}avutil-${LIBAVUTIL_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
+		set(LIBPOSTPROC_LIB cyg${LIBAVCODEC_ADM}postproc-${LIBPOSTPROC_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
+		set(LIBSWSCALE_LIB cyg${LIBAVCODEC_ADM}swscale-${LIBSWSCALE_VERSION}${CMAKE_SHARED_LIBRARY_SUFFIX})
 	elseif (UNIX)
 		set(LIBAVCODEC_LIB lib${LIBAVCODEC_ADM}avcodec${CMAKE_SHARED_LIBRARY_SUFFIX}.${LIBAVCODEC_VERSION})
 		set(LIBAVFORMAT_LIB lib${LIBAVCODEC_ADM}avformat${CMAKE_SHARED_LIBRARY_SUFFIX}.${LIBAVFORMAT_VERSION})
--- origsrc/avidemux_2.5.1/cmake/patches/config.mak.diff	2009-05-23 15:19:56.000000000 -0500
+++ src/avidemux_2.5.1/cmake/patches/config.mak.diff	2009-11-10 21:39:45.691438300 -0600
@@ -1,11 +1,11 @@
---- config.mak.old	2009-05-01 19:26:13.808782802 +0200
-+++ config.mak	2009-05-01 18:10:25.000085291 +0200
-@@ -60,7 +60,7 @@
- BUILD_SHARED=yes
- PIC=-fPIC -DPIC
- LIBTARGET=
--SLIBNAME=$(SLIBPREF)$(FULLNAME)$(SLIBSUF)
-+SLIBNAME=$(SLIBPREF)ADM5$(FULLNAME)$(SLIBSUF)
- SLIBNAME_WITH_VERSION=$(SLIBNAME).$(LIBVERSION)
- SLIBNAME_WITH_MAJOR=$(SLIBNAME).$(LIBMAJOR)
- SLIB_CREATE_DEF_CMD=
+--- config.mak.old	2009-11-10 20:51:34.116158200 -0600
++++ config.mak	2009-11-10 20:52:39.536248500 -0600
+@@ -36,7 +36,7 @@
+ LIBPREF=lib
+ LIBSUF=.a
+ LIBNAME=
+-SLIBPREF=cyg
++SLIBPREF=cygADM5
+ SLIBSUF=.dll
+ EXESUF=.exe
+ EXTRA_VERSION=

